package com.hrens.utils;

import org.bukkit.plugin.java.JavaPlugin;

public class MessageConfig extends FileConfig{
    public MessageConfig(JavaPlugin plugin, String name) {
        super(plugin, name);
    }
}
